import { DateShortPipe } from './date-short.pipe';

describe('DateShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateShortPipe();
    expect(pipe).toBeTruthy();
  });
});
