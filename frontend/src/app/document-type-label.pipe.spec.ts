import { DocumentTypeLabelPipe } from './document-type-label.pipe';

describe('DocumentTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
